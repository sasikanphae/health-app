{
  "name": "health-app",
  "version": "0.1.0",
  "description": "แอพติดตามสุขภาพประจำวัน (PWA)",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test tests/*.test.js"
  }
}
